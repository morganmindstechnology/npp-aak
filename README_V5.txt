NPP A.A.K. V5 - Typed Electoral Area and Branch

This version changes the membership form so applicants can TYPE:
- Electoral area (required)
- Branch (optional)

Run migration_free_text.sql once in Supabase SQL Editor.

Important:
1. V4/V5 Supabase URL and publishable key are embedded in index.html/admin.html.
2. Do NOT put a service-role or secret key in frontend files.
3. For browser testing, host the files on HTTPS rather than opening index.html directly with file://.
4. AppCreator24 can point its Web section to the HTTPS URL where index.html is hosted.
