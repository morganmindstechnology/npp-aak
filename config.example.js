// Supabase configuration for the web app.
// Copy this file to config.js and replace the placeholders.
//
// Use the project's publishable/anon key only.
// NEVER place the Supabase service-role or secret key in a browser app.

window.NPP_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_PUBLISHABLE_KEY: "YOUR-PUBLISHABLE-KEY"
};
